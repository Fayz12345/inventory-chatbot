# Inventory Chatbot — runtime dependencies (ADO #198 / 1D.10).
# Versions pinned to what the working venv runs. Jinja2 + Werkzeug come
# transitively with Flask; httpx/apify-shared come with anthropic/apify-client.
Flask==3.1.3
python-dotenv==1.2.1
pyodbc==5.3.0
requests==2.32.5
python-amazon-sp-api==2.1.8
apify-client==1.12.2
openpyxl==3.1.5
anthropic==0.102.0
sqlglot==30.12.0

# Dev / test only
pytest==8.4.2
